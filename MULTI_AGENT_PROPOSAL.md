# مقترح تحويل هادي إلى Multi-Agent (نقطة 10)

> **الخلاصة التنفيذية:** هادي بالفعل نص multi-agent من غير ما نسميه كده. المقترح:
> تقنين الأدوار الموجودة كوكلاء رسميين بصلاحيات أدوات أضيق (least privilege)،
> وإضافة orchestrator خفيف — على 3 مراحل، كل مرحلة بتتقاس بالـ evals قبل ما ننتقل.
> **التوصية: ننفذ المرحلة 1 بس دلوقتي** — 80% من الفايدة بـ 20% من المخاطرة.

---

## 1. الوضع الحالي (بذور الـ multi-agent موجودة فعلًا)

| الدور الفعلي | الكود الحالي | الموديل | ملاحظة |
|---|---|---|---|
| بوابة الحضور (أتكلم؟) | `ambient_gate.py` | Haiku | وكيل قرار مستقل بالفعل |
| المحاور الرئيسي | `hadi_engine.run_agent` | Sonnet | كل الأدوات + كل التعليمات |
| مستخرج الترياج النظيف | `hadi_engine.run_clean_json` | Sonnet | جلسة معزولة بدون شخصية |
| كتّاب التقارير | `daily_digests.py` | Sonnet | نداء مقيد ببرومبت ثابت |
| الحكم | `eval_judge.py` | Sonnet | مقيّم مستقل بالعينة |

المشكلة الوحيدة الحقيقية: **المحاور الرئيسي بيشيل كل حاجة** — كل الأدوات، كل التعليمات، كل الصلاحيات، في جلسة واحدة. ده بيكبّر الـ prompt، وبيوسّع سطح الخطأ والأمان (أي حقن بيوصل لكل الأدوات)، وبيخلي موديل واحد غالي بيعمل شغل بعضه يستاهل موديل أرخص.

## 2. المعماري المقترح

```
Discord msg ──> Router (orchestrator خفيف — كود + Haiku)
                 │
    ┌────────────┼──────────────┬───────────────┐
    ▼            ▼              ▼               ▼
 Presence     Conversation    Ops Agent      Analyst
 (موجود:      (Sonnet —       (Sonnet —      (Sonnet —
 ambient_gate) شخصية كاملة،    أدوات ADO      posthog + snapshot
 صامت/رياكشن   ذاكرة، بدون     كتابة فقط +     قراءة فقط +
 /تصعيد        كتابة ADO)      ado_fields)     قوالب التحليل)
                 │
              محتاج تذكرة/تحليل؟ ──> handoff بالـ Task tool للـ specialist
```

**الأدوار:**

1. **Router:** كود حتمي أولًا (منشن/DM/تريجر ترياج زي دلوقتي) + بوابة Haiku للـ ambient (موجودة). مفيش موديل جديد هنا.
2. **Conversation Agent:** هادي الاجتماعي — الشخصية والذاكرة والمعرفة، **من غير صلاحيات كتابة ADO خالص**. لو الطلب فيه تذكرة → بينادي Ops.
3. **Ops Agent:** الوحيد اللي معاه `ado_cli.py` كتابة. تعليماته صفحة واحدة (SOP التذاكر + ado_fields) — برومبت صغير = أخطاء أقل وكاش أفضل. أي طلب بيوصله **مُهيكل** (JSON من الـ Conversation) مش نص حر — نقطة تفتيش طبيعية ضد الحقن.
4. **Analyst Agent:** قراءة PostHog + snapshot + قوالب `HADI_ANALYSIS_INSTRUCTIONS` — للتقارير والتحليلات الثقيلة. ممكن يترقى لـ Opus وقت الريليز.
5. **Scribe/Judge:** زي ما هما (`daily_digests`, `eval_judge`) — بيتسموا رسميًا وكلاء وبيتوثقوا كده.

**آلية التنفيذ بالـ Claude Agent SDK:** تعريف subagents بـ `agents={"ops": AgentDefinition(prompt=..., tools=[...], model=...)}` في `ClaudeAgentOptions`، والـ Conversation بينادي Ops عبر أداة `Task` — نفس الـ SDK الحالي، مفيش بنية جديدة. صلاحيات كل وكيل بتتحدد بـ `allowed_tools` + allow-list منفصل — **وده بيحل جزء كبير من F4 معماريًا**: حتى لو حقن نجح في الـ Conversation، مفيش عنده أدوات كتابة يسيء استخدامها.

## 3. Risk vs Benefit

| | التفصيل |
|---|---|
| **Benefits** | أمان: least-privilege حقيقي لكل دور (توصية OWASP ASI) • تكلفة: Haiku للبوابة والراوتينج، Sonnet للحوار، الغالي بس للتحليل • جودة: برومبت متخصص أصغر = دقة أعلى وكاش أثبت • صيانة: تعديل SOP التذاكر مايلمسش شخصية هادي والعكس |
| **Risks** | زمن استجابة أطول في الـ handoffs (نداء إضافي للتذاكر) • فقدان سياق بين الوكلاء (المحادثة عارفة حاجات الـ Ops مش شايفها) • تعقيد debugging (تتبع قرار عبر 2-3 وكلاء) • هندسة زيادة لتيم فرد واحد بيصونها |
| **التخفيف** | الـ snapshot والذاكرة مشتركين أصلًا (نفس SQLite/ملفات) فالسياق الحرج متاح للكل • الطلب المُهيكل بين الوكلاء بيتسجل JSONL (trace) • كل مرحلة وراها eval قبل/بعد — لو الجودة وقعت بنرجع |

## 4. خطة التنفيذ المرحلية

**المرحلة 1 — تقنين الموجود (أسبوع، مخاطرة شبه صفر) ← ابدأ هنا:**
- تسمية الأدوار الحالية رسميًا (persona/gate/scribe/judge) وتوثيق حدود كل واحد.
- فصل allow-lists: ملف صلاحيات للـ bot الرئيسي وملف أضيق لسكربتات الروتين.
- قياس baseline بالـ evals الحالية (خط الأساس اللي المراحل الجاية بتتقارن بيه).

**المرحلة 2 — فصل الـ Ops Agent (أسبوعين):**
- subagent للتذاكر بـ `Task` tool: الـ Conversation بيبعتله JSON مُهيكل (نوع/عنوان/وصف/حقول).
- شيل صلاحيات كتابة ADO من الـ Conversation.
- Gate النجاح: نفس معدل نجاح التذاكر في الـ evals + مفيش زيادة ملحوظة في الـ latency.

**المرحلة 3 — الـ Analyst وترقية الراوتر (شهر، حسب الحاجة الفعلية):**
- Analyst منفصل للتقارير الكبيرة (release/regression analysis).
- راوتر يتعلم من لوج القرارات (ambient_gate.jsonl) — تحسين الـ pre-filter بالداتا.

## 5. القرار المطلوب من آسر

مفيش قرار مستعجل — المقترح ده وثيقة تصميم. لما تيجي تنفذ: ابدأ بالمرحلة 1، وقيس، وبعدين قرر لو المرحلة 2 مستاهلة. لو التيم كبر أو التذاكر اليومية عدت ~30/يوم، المرحلة 2 هتدفع تمن نفسها بسرعة.
