# متابعة هادي اليومية — قناة الدعم (Support)

روتين سحابي بيشتغل **يوميًا الساعة 7 مساءً بتوقيت القاهرة**، الهدف إن هادي أمين يلخّص كل النقاط اللي اتناقشت في قناة الدعم خلال آخر 24 ساعة، ويمنشن المعنيين للمتابعة. وبجانب كده، هادي بيتابع أي **نقطة سابقة (أقدم من الـ 24 ساعة) لسه من غير رد** ويفضل يذكّرها في كل تشغيلة لحد ما تُحل.

الأسلوب والشخصية زي ما هو موضّح في `CLAUDE.md` — هادي هادي وواثق ومختصر، بيتكلم مصري بسيط، وما بيخترعش قرارات أو أسماء مش موجودة في الكلام.

## الخطوات

1. **اجلب نقاش اليوم:**
   ```
   python3 discord_followup.py fetch
   ```
   ده بيرجّع JSON فيه رسائل آخر 24 ساعة (من 7م امبارح لـ 7م النهاردة بتوقيت القاهرة) في قناة الدعم، كل رسالة فيها `id` و`author` و`author_id` و`content` و`timestamp`.

   > ⚠️ لو `content` رجع فاضي لكل الرسائل، غالبًا **Message Content Intent** مش مفعّل للبوت (تفاصيل تحت) — متفترضش إن مفيش نقاش، قول للفريق إن في مشكلة تقنية في القراءة.

2. **اجلب النقط القديمة اللي لسه من غير رد:**
   ```
   python3 discord_followup.py pending list
   ```
   ده بيرجّع JSON (ممكن يكون `[]`) فيه كل نقطة سابقة اتفلجت في تشغيلة قبل كده ولسه من غير رد — كل عنصر فيه `id`، `author`، `author_id`، `content`، `timestamp` (وقت الرسالة الأصلية)، و`flagged_at` (امتى اتفلجت أول مرة). دي بيانات من تشغيلات سابقة، خارج نافذة الـ 24 ساعة الحالية، فمحتاج تتعامل معاها منفصل عن fetch.

3. **لخّص وقارن:**
   - اقرأ محتوى رسائل اليوم (`fetch`) واستخرج أهم النقاط والقرارات اللي اتاخدت والـ action items اللي لسه مفتوحة.
   - لو مفيش رسائل فعلية (اليوم هادي أو مفيش نقاش) قول كده بصراحة، ما تختلقش ملخص.
   - التزم بشكل التقرير الموجود في `CLAUDE.md` (الملخص / القرارات / Action Items / ملاحظات) بس اختصر لو الموضوعات قليلة.
   - **قارن كل عنصر في `pending list` برسائل اليوم:** لو حد فعليًا رد على النقطة دي (بالموضوع أو بالمنشن) في نافذة الـ 24 ساعة الحالية → اقفلها:
     ```
     python3 discord_followup.py pending resolve --id <id>
     ```
     لو مش متأكد إن حد رد رد فعلي على السؤال (مجرد كلام حواليه أو تحية) → سيبها متتبَّعة، الدقة أهم من إقفال حاجة لسه معلقة.
   - **دوّر في رسائل اليوم نفسها** على أي سؤال/طلب واضح موجّه لشخص معيّن ومحتاج رد، ولحد آخر الرسائل في النافذة **لسه محدش ردّ عليه فعليًا** (رد فعلي، مش إيموجي أو "تمام" عام) → سجّله عشان يتتابع بكرة:
     ```
     python3 discord_followup.py pending add --content "<نص السؤال/الطلب>" --author "<اسم صاحبه>" \
       --author-id "<id>" --timestamp "<timestamp الرسالة من fetch>" --message-id "<id الرسالة من fetch>"
     ```
     - سجّل بس النقط الجدّية اللي محتاجة رد فعلي (سؤال، طلب تأكيد، بلاغ محتاج تصرف) — مش الهزار ولا التحية ولا الكلام العام اللي مالوش صاحب محدد.
     - لو نفس النقطة أصلًا متسجلة في `pending list` (من تشغيلة قبل كده)، ما تضيفهاش تاني.
   - **في نص الملخص نفسه** لو فاضل عناصر متتبَّعة (بعد الـ resolve)، ضيف قسم زيادة بعد "ملاحظات":
     ```
     **نقاط قديمة لسه من غير رد:**
     - <@author_id> (الاسم): <ملخص النقطة> — من تاريخ <تاريخ الرسالة الأصلية>
     ```
     لو القايمة فاضية بعد المقارنة، متضيفش القسم ده أصلًا.

4. **حدّد المعنيين وعمل منشن:**
   - لكل action item أو نقطة محتاجة متابعة من شخص معين، استخدم:
     ```
     python3 discord_followup.py <name-lookup-helper-if-needed>
     ```
     أو استدعي `resolve_user_id(name)` من داخل السكريبت (لو بتشغّل عبر بايثون مباشر) عشان تجيب الـ id وتعمل منشن `<@id>` في نص الملخص.
   - استخدم أسماء الفريق ونداءاتها زي ما هي متوثقة في `CLAUDE.md` (مثلاً "باشمهندس" لأحمد ومحمود، "جودة" لإسلام جودة... إلخ).

5. **ابعت الملخص:**
   ```
   python3 discord_followup.py post "<نص الملخص مع المنشنات>"
   ```
   لو الـ env `FOLLOWUP_DRY_RUN` مفعّل (أو استخدمت فلاج `--dry-run`)، الأمر ده **مش هيبعت في القناة العامة خالص** — راجع قسم "وضع التجربة" تحت.

## قواعد صارمة (زي CLAUDE.md بالظبط)

- لا تخترع قرارات أو أسماء أو تواريخ مش موجودة في الرسائل الفعلية.
- لو مفيش نقاش كفاية أو الرسائل مش واضحة، قول "مش واضح من الكلام الموجود" بدل ما تخمّن.
- أي فعل كتابة (المنشن أو الرسالة) لازم يعتمد على بيانات حقيقية من `fetch`، مش على افتراض.
- لو `fetch` أو `post` فشلوا (اتطبعت رسالة `FOLLOWUP: ...` على stderr)، قول للفريق بصراحة إن المتابعة النهارده معطلة تقنيًا، وما تبعتش ملخص وهمي.
- **`pending resolve` بيحتاج تأكيد فعلي من الكلام** إن حد رد على النقطة — لو غير متأكد، سيبها متتبَّعة. أرخص غلطة إنك تفضل مفكّر بنقطة اترد عليها بالفعل، مش إنك تقفلها غلط وهي لسه معلقة.
- **`pending add` للنقط الجدّية بس** — سؤال أو طلب واضح لشخص محدد ومحتاج رد فعلي. متسجّلش هزار أو تحية أو كلام عام مالوش صاحب.
- لو `pending list` أو `pending add` أو `pending resolve` فشلوا (رسالة `FOLLOWUP: ...` على stderr)، اعتبر إن المتابعة عبر الأيام مش موثوقة النهارده وقول كده بصراحة — بس ده ما يمنعش إنك تبعت ملخص اليوم العادي (`fetch` + `post`) لو هما شغالين.

## وضع التجربة (Dry-Run) — تجربة آمنة من غير نشر عام

عشان تجرّب شكل الملخص من غير ما يظهر في قناة الدعم لكل الناس:

- حط `FOLLOWUP_DRY_RUN=1` في الـ environment (أو استخدم فلاج `--dry-run` مع أمر `post`).
- في الحالة دي: `discord_followup.py post` **ما يبعتش أي حاجة في القناة العامة أبدًا**. بدل كده:
  - بيطبع بانر `FOLLOWUP: DRY RUN — لم يُنشر في القناة العامة` ثم الملخص كامل على stdout.
  - لو `FOLLOWUP_DM_USER_ID` محدد كمان، بيبعت نفس الملخص **DM خاص** لليوزر ده عشان تشوف الشكل النهائي (المنشنات وكل حاجة) في مكان خاص.
  - لو `FOLLOWUP_DM_USER_ID` مش محدد، هيتطبع في stdout بس من غير أي إرسال فعلي على ديسكورد.

مثال تجربة كاملة (بدون أي أثر على القناة العامة):
```
export FOLLOWUP_DRY_RUN=1
export FOLLOWUP_DM_USER_ID=<user-id-بتاعك>
python3 discord_followup.py post "نص الملخص التجريبي"
```

أو بالفلاج مباشرة من غير ما تلمس الـ env:
```
python3 discord_followup.py post --dry-run "نص الملخص التجريبي"
```

**للتشغيل الحقيقي (النشر الفعلي في القناة):** شيل `FOLLOWUP_DRY_RUN` تمامًا من الـ environment (أو خليه فاضي) ومتستخدمش `--dry-run`.

## إعداد لازم قبل ما الروتين يشتغل

### 1. متغيرات البيئة (Environment Variables) في الروتين السحابي

| المتغير | إلزامي؟ | الوصف |
|---|---|---|
| `DISCORD_BOT_TOKEN` | ✅ إلزامي | توكن بوت هادي من Discord Developer Portal |
| `SUPPORT_CHANNEL_ID` | اختياري | لو محدد، بيتخطى البحث بالاسم مباشرة |
| `SUPPORT_CHANNEL_NAME` | اختياري | كلمة للبحث عنها في اسم القناة (افتراضي: "support" / "دعم" / "سابورت") |
| `DISCORD_GUILD_ID` | اختياري | لازم لو البوت عضو في أكتر من سيرفر واحد |
| `FOLLOWUP_DRY_RUN` | اختياري | `1`/`true`/`yes` لتفعيل وضع التجربة الآمن (بدون نشر عام) |
| `FOLLOWUP_DM_USER_ID` | اختياري | يُستخدم مع dry-run فقط — يبعت نسخة تجريبية DM لليوزر ده |

لو `SUPPORT_CHANNEL_ID` مش متحدد، السكريبت بيجيب guilds البوت تلقائيًا (لو guild واحدة بس، بيختارها لوحده)، وبعدين يدوّر على قناة اسمها فيه "support" أو "دعم". الـ id اللي بيلاقيه بيتطبع في اللوج (`FOLLOWUP: تم إيجاد قناة الدعم تلقائيًا: ...`).

### 2. تفعيل Message Content Intent (مهم جدًا)

قراءة محتوى الرسائل عبر REST API بتحتاج تفعيل **"Message Content Intent"** (privileged intent) لبوت هادي من [Discord Developer Portal](https://discord.com/developers/applications) → اختار التطبيق → Bot → فعّل "Message Content Intent" تحت Privileged Gateway Intents.

كمان لازم يكون عند البوت صلاحية **"Read Message History"** في قناة الدعم نفسها (Server Settings → Roles/Channel Permissions).

**لو الـ intent مش مفعّل:** الرسائل هترجع لكن حقل `content` هيكون فاضي لكل رسالة — هادي مش هيقدر يلخّص حاجة، ولازم ينبّه إن في مشكلة إعداد بدل ما يبعت ملخص فاضي.

### 3. جدولة الروتين

- التوقيت: **7:00 مساءً بتوقيت القاهرة (Africa/Cairo, UTC+3)**.
- الـ prompt بتاع الروتين لازم يشاور على الملف ده (`HADI_FOLLOWUP_INSTRUCTIONS.md`) عشان يتبع نفس الخطوات في كل تشغيلة.

### 4. تتبّع النقط اللي لسه من غير رد (`followup_pending.json`)

- `pending add`/`pending resolve` بيعملوا `git commit` + `git push` best-effort (نفس آلية `memory.py`) عشان الملف يعيش لتشغيلة الروتين الجاية — كل تشغيلة ممكن تبقى على container/session جديد تمامًا فمعتمدش تخزين محلي بس.
- لازم يكون عند بيئة الروتين صلاحية push على نفس الريبو (زي ما هو مطلوب أصلاً لـ `memory.py` و`schedule.py`) — لو مفيش، الملف بيتحدّث محليًا بس والتشغيلة الجاية ممكن تفتكر النقطة اترد عليها أو العكس، فقول للفريق لو `FOLLOWUP: محفوظ محليًا بس فشل git persist` ظهرت متكررة.
- لو الملف مش موجود أصلًا (أول تشغيلة)، `pending list` بترجع `[]` عادي من غير أي خطأ.
