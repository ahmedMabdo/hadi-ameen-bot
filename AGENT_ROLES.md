# أدوار وكلاء هادي وحدودهم — مرحلة 1

> **الغرض:** هادي بالفعل أكتر من وكيل واحد، بس ده كان ضمني — الأدوار اتولدت مع
> الوقت ومحدش وثّق حدودها. الملف ده بيسميها رسميًا ويحدد **مين معاه إيه**.
>
> **القاعدة الحاكمة:** أي إضافة صلاحية لأي وكيل لازم تعدي على الملف ده الأول.
> لو صلاحية اتضافت من غير ما السطر هنا يتغير — دي غلطة، مش اختصار.
>
> المرجع المعماري: `MULTI_AGENT_PROPOSAL.md`. القالب: `setup/settings.local.json`.

---

## 1. جدول الأدوار (الواقع المشحون، متحقق من الكود)

| # | الوكيل | الكود | الموديل | شغله | معاه إيه |
|---|--------|-------|---------|------|----------|
| 1 | **بوابة الحضور** | `ambient_gate.py` | Haiku | يقرر: أسكت / رياكشن / أرد | **مفيش أدوات خالص — مفروض في الأمر** بـ `--allowedTools "" --permission-mode plan` (اتضاف 2026-07-26؛ قبله كان الوصف ده وعد في التوثيق والكود بينده الـ CLI بالافتراضيات). `cwd=/tmp` |
| 2 | **المحاور** | `hadi_engine.run_agent` | Sonnet | يتكلم مع الناس — الشخصية والذاكرة والمعرفة | كل الـ allow-list (قراءة **وكتابة** — ده اللي المرحلة 2 هتقسمه) |
| 3 | **المستخرج** | `hadi_engine.run_clean_json` | Sonnet | يطلّع JSON نضيف للترياج | جلسة معزولة: `setting_sources=[]` — **مفيش CLAUDE.md ولا شخصية**، `cwd=/tmp`، 10 أدوار |
| 4 | **الكاتب** | `daily_digests.py` | Sonnet | التقارير الأربعة المجدولة | برومبت ثابت مقفول + `PERSONA_CORE.md`. بينده `posthog_cli health` قبل أي نشر |
| 5 | **الحكم** | `eval_judge.py` | Sonnet | تقييم أسبوعي بعينة | قراءة `eval_store` بس. بيقيّم، مايعدّلش |

**ملاحظة على 4 و5:** الاتنين بيتنادوا من **تايمر systemd**، مش من هادي. وعشان كده
`daily_digests.py` و`eval_report.py` و`eval_judge.py` في `deny` — لو هادي نداهم
بنفسه هيتبعت تقرير مكرر للقناة.

---

## 2. حدود الصلاحيات (اللي اتقسم في مرحلة 1)

الـ allow-list اتقسمت مجموعتين **بالنية مش بالصدفة**:

### قراءة — المحاور محتاجها عشان يجاوب بمصدر
```
knowledge_store.py   بحث في قاعدة المعرفة
posthog_cli.py       أرقام المنتج لايف (بحارس LIVE/STALE/DOWN)
ado_snapshot.py      حالة البوردات والسبرنت من الكاش
sprints_sync.py      عرض ملف السبرنت
sprint_intake.py     مواعيد الإيفنتات
memory.py            الذاكرة (بحراس memory_guard)
schedule.py          التذكيرات
WebSearch, WebFetch
```

### كتابة ونشر — دي اللي هتتنقل لـ Ops Agent في المرحلة 2
```
ado_cli.py               إنشاء work items + إرفاق ميديا (إضافة فقط)
po_channel_cr.py         إنشاء CR من قناة الـ PO
ado_alerts.py            تنبيهات
discord_followup.py      نشر في قناة السابورت
discord_mars_results.py  نشر نتائج الأوتوميشن
```

**اتشال 2026-07-26:** `discord_orderpo.py` → `legacy/` (حقول CR ناقصة +
تكرار مع `po_channel_cr` و`channel_triage` على نفس القناة).

```
```

**ليه التقسيم ده مهم دلوقتي حتى قبل المرحلة 2:** الحدود بقت **مرسومة**. فصل
الـ Ops Agent بعد كده بقى نقل سطور من مجموعة لمجموعة، مش إعادة تصميم.

---

## 3. طبقات الأمان (مرتبة من الأقوى للأضعف)

1. **الـ allow-list** (`.claude/settings.local.json`) — الجدار الحقيقي. أي أمر
   مش في القايمة مايتنفذش. ده اللي بيتعتمد عليه.
2. **حراس الـ PreToolUse** (`hadi_engine._bash_guard` / `_file_guard`) — طبقة
   دفاع في العمق ضد أنماط خطرة (قراءة `.env`، `/proc/*/environ`، تمرير
   `HADI_MEMORY_ADMIN`). **مش** بديل للقايمة.
3. **بوابة الطرفية** (`memory_guard.is_human`) — القواعد السلوكية
   (`type=procedural`) مابتدخلش الذاكرة غير بموافقة بني آدم على SSH.
   `sys.stdin.isatty()` بيبقى False جوه جلسة هادي — منع حتمي في الكود.
4. **التعليمات** (`CLAUDE.md`, `HADI_PERSONA.md`) — أضعف طبقة. توجيه سلوك،
   **مش** ضمان أمان. عمرنا ما نعتمد عليها لوحدها في حاجة حساسة.

---

## 4. الدرس اللي اتعلمناه (2026-07-24)

القايمة على السيرفر كانت **متفرّعة عن الريبو**، والنتيجة قدرتين موثّقتين كانوا
**مقفولين من غير ما حد ياخد باله**:

| الأداة | CLAUDE.md بتقول | الواقع كان |
|---|---|---|
| `knowledge_store.py` | «ابدأ بالبحث ده قبل ما تفتح أي ملف» | مش في القايمة → **قاعدة المعرفة كلها ميتة** |
| `posthog_cli.py` | «لأي سؤال عن أرقام التطبيق استخدم الأداة» | مش في القايمة → كل سؤال أرقام لايف بيفشل |

وكمان سطر ميت: `component_evals.py` (الملف اتقسم لـ `eval_judge.py` +
`eval_report.py` ومحدش حدّث القايمة).

**الخلاصة العملية:** تعليمة في CLAUDE.md من غير صلاحية مقابلة = **وعد كاذب**.
هادي بيحاول وبيتمنع، والمستخدم بيفتكره مش عارف أو بيهرب من السؤال.

**عشان مايتكررش:**
- أي أداة جديدة في CLAUDE.md لازم يتضاف معاها سطر في `setup/settings.local.json`
  في **نفس الكوميت**.
- فحص دوري بسيط (يتشغل بعد أي تعديل على CLAUDE.md):

```bash
# كل أداة متعلّمة، هل هي مسموحة؟
comm -23 \
  <(grep -oE "python3 [a-z_]+\.py" CLAUDE.md | awk '{print $2}' | sort -u) \
  <(grep -oE "Bash\(python3 [a-z_]+\.py" .claude/settings.local.json | sed 's/.*python3 //' | sort -u)
# أي اسم بيطلع هنا = تعليمة من غير صلاحية
```

---

## 5. خط الأساس (الخطوة التالتة في مرحلة 1)

الآلية موجودة أصلًا في `eval_runner.py` — مفيش كود جديد:

```bash
cd ~/hadi-ameen-bot
python3 eval_runner.py validate    # فحص السكيما (مجاني، من غير نداء موديل)
python3 eval_runner.py run         # 16 حالة — تشغيلات Claude حقيقية
python3 eval_runner.py baseline    # علّم التشغيلة دي كخط أساس
```

وبعد أي تغيير جاي:

```bash
python3 eval_runner.py run && python3 eval_runner.py diff
```

`diff` بيقولك **أسماء** الحالات اللي اترجّعت (regressions) واللي اتصلحت — مش
نسبة مئوية بس.

**شرطان مهمان لخط الأساس:**

1. **لازم يتشغل على السيرفر** مش على أي بيئة تانية — الحالات بتقيس سلوك هادي
   بالـ CLAUDE.md والـ allow-list الحقيقيين. خط أساس من بيئة تانية بيقيس نظام تاني.
2. **لازم يتشغل بعد إصلاح الصلاحيات** (2026-07-24). أي تشغيلة قبل كده كانت
   بتقيس نظام `knowledge_store` و`posthog_cli` مقفولين فيه — رقم لنظام مكسور،
   مايصلحش مرجع نقارن بيه.
