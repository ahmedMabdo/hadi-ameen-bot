requests
claude-agent-sdk>=0.2.122

# discord_bot.py — متماشٍ مع discord.py 2.5+
# MessageReferenceType.message_snapshots (الرسائل المحذوفة)
discord.py>=2.5.0

# قراءة .env في discord_bot / hadi_engine / posthog_cli / daily_digests
python-dotenv>=1.0.0

# استخراج نص المرفقات (file_extract.py)
# يبحث في البوت عن محتوى المستند بدل رسالة الخطأ
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0

# إنشاء تقارير PDF الخاصة بـ PostHog
Pillow>=10.0.0
weasyprint>=69.0
# تحويل PDF → PNG لـ issues_table_png.py (WeasyPrint 53+ شال write_png)
PyMuPDF>=1.23.0

# تقرير صوت الفيديو محليًا (اختياري)
# يحتاج ffmpeg مثبتًا على السيرفر
faster-whisper>=1.0.0
