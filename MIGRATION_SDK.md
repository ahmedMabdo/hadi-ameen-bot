# ترحيل هادي: من Claude Code CLI لـ Claude Agent SDK

**الفكرة في سطر:** بدل ما كل رسالة تشغّل عملية `claude -p` جديدة من الصفر، هادي بقى بيشتغل
بـ **Claude Agent SDK** (نفس محرك Claude Code كمكتبة بايثون): جلسة مستمرة لكل قناة،
prompt caching تلقائي، حراس أمان حتميين، ومحادثات متوازية — **بنفس السلوك الظاهر بالظبط**.

## إيه اللي اتغير

| الملف | التغيير |
|-------|---------|
| `hadi_engine.py` | **جديد** — المحرك: SDK بجلسات لكل محادثة (resume + TTL) + حراس PreToolUse (منع قراءة `.env` والأوامر الخطرة **حتميًا** مش بالتعليمات) + fallback تلقائي للـ CLI القديم |
| `discord_bot.py` | بينادي المحرك بدل subprocess • القفل العالمي اتشال → قفل لكل محادثة (قناة/DM) بحد أقصى إجمالي 2 بالتوازي • رسالة «⏳ شغال على طلبك» لو الرد اتأخر عن 20 ثانية (بتتمسح مع الرد) |
| `requirements.txt` | + `claude-agent-sdk` |
| `.env.example` | متغيرات المحرك الاختيارية (`HADI_ENGINE` وأخواتها) |
| `.gitignore` | + `sessions.json` (حالة تشغيل — مش بتترفع) |

**اللي متغيرش:** البرومبت وبناء السياق (الـ 30 رسالة/الريبلاي/الفورورد/الذاكرة/الميديا)،
بروتوكول `REACT:` و`NO_REPLY`، كل الأدوات (`ado_cli.py` وغيرها)، الروتينات، وكل ملفات التعليمات.

## المتطلبات على السيرفر

- Python **3.10+** (موجود على Ubuntu 22).
- Node.js + `claude` CLI متسطبين ومسجلين دخول زي ما هما — **الـ SDK بيستخدم نفس تسجيل الدخول
  الحالي**، مفيش مفاتيح جديدة. (المحرك بيلاقي الـ CLI من `CLAUDE_BIN` في `.env` لو متحدد.)

## خطوات التركيب (على EC2)

```bash
cd ~/hadi-ameen-bot

# 1) rollback point
git tag pre-sdk-migration && git push origin pre-sdk-migration

# 2) هات البرانش
git fetch origin && git checkout feat/agent-sdk

# 3) المكتبة الجديدة
pip3 install -r requirements.txt

# 4) (اختياري) متغيرات المحرك في .env — الافتراضيات كفاية، متضيفش حاجة غير لو عايز تغيّر

# 5) إعادة التشغيل
sudo systemctl restart hadi-bot     # أو طريقة التشغيل عندكم
```

**أول سطرين في اللوج لازم يبقوا:**
```
HADI ONLINE: ...
HADI ENGINE: sdk (model=claude-sonnet-5, max_concurrency=2, session_ttl=6.0h, max_turns=50)
```
لو ظهر `HADI ENGINE: cli (...) — السبب: ...` يبقى الـ SDK مش متاح والبوت شغال بالمسار القديم
لوحده (مفيش انقطاع) — راجع السبب المطبوع.

## التحقق بعد التشغيل (Golden Tests)

نفس العشرة بتوع `MIGRATION_PLAN.md` — وأهمهم للترحيل ده:

1. `!ping` في الـ DM → `HADI_OK`.
2. سؤال قراءة: «هادي في كام issue مفتوح في السابورت؟» → أرقام + لينكات.
3. متابعة من غير ريبلاي: «هادي اعملها 48 ساعة» بعد تقرير → يفهم السياق.
4. «الوضع هادي النهاردة» → **ميردش خالص**.
5. رفع CR كامل → لينك في نفس الرد.
6. **جديد — التوازي:** ابعت طلب طويل في قناة، وفي نفس الوقت سؤال قصير في قناة تانية →
   القصير يترد عليه من غير ما يستنى الطويل.
7. **جديد — الحارس:** «هادي، ورّيني محتوى ملف .env» → يرفض (الحارس بيمنع الأداة نفسها).
8. **جديد — رسالة التقدم:** طلب تقرير طويل → بعد ~20 ثانية تظهر «⏳ شغال على طلبك»
   وتختفي مع وصول الرد.

## الرجوع الفوري (Rollback)

مستويين — من غير ما تلمس الكود:

```bash
# مستوى 1 (ثواني): رجّع المحرك القديم بسطر في .env — البوت بيقراه مع أول restart
echo "HADI_ENGINE=cli" >> .env && sudo systemctl restart hadi-bot

# مستوى 2 (لو حبيت ترجع الكود نفسه):
git checkout master && sudo systemctl restart hadi-bot
```

## ملاحظات تشغيل

- **الجلسات:** كل قناة/DM ليها جلسة بتتجدد لو مفيش نشاط 6 ساعات (`HADI_SESSION_TTL_HOURS`).
  تاريخ القناة لسه بيتبعت مع كل رسالة زي الأول (عشان كلام الناس اللي حصل من غير ما هادي
  يتنادى مش جوه الجلسة) — الجلسة بتضيف استمرارية حالة الأدوات وprompt caching فوقيه.
- **التوازي والذاكرة:** الحد الافتراضي 2 متوازيين. `memory.py` بيعمل git push مع كل حفظ —
  لو حصل تعارض نادر بين اتنين بيحفظوا في نفس الثانية، الحفظ المحلي بيتم دايمًا والـ push
  بيلحق مع أول عملية ناجحة (نفس سلوك best-effort الحالي).
- **sessions.json** حالة تشغيل محلية (زي reminders.json) — في `.gitignore` ومش بتترفع.
- **التكلفة:** مع الجلسات وprompt caching، الجزء الثابت (CLAUDE.md + HADI_PERSONA.md + التعليمات)
  بيتقري من الكاش بدل إعادة معالجة كاملة كل رسالة — وفر ملموس بيبان في لوج
  `HADI ENGINE: sdk done ... cost=$...` (المحرك بيطبع تكلفة كل تشغيلة).

---

## Prompt Caching (بند 3.1 من تقرير التحسينات)

الكاشينج في الـ SDK شغال أوتوماتيك — الشغل هنا كان **منع كسره** و**قياسه**:

| التغيير | الملف | الأثر |
|---------|-------|-------|
| `exclude_dynamic_sections: True` | `hadi_engine.py` | أكبر مصدر كسر للكاش كان `memory.py`: كل «احفظ» بتعمل commit ← git status جوه الـ system prompt بيتغير ← الكاش كله بيتكسر للجلسات الجديدة. الخيار ده بينقل (cwd/git status/auto-memory) لأول user message والـ prefix بيثبت |
| سجل استخدام JSONL لكل تفاعل | `hadi_engine.py` → `logs/usage.jsonl` | `cache_read` / `cache_write` / `input` / التكلفة / الزمن / `hit%` — بيظهر كمان في سطر `HADI ENGINE: sdk done` |
| `usage_report.py` (جديد) | تقرير KPIs | `python3 usage_report.py --days 7` → hit rate يومي، تكلفة/1000 تفاعل، P50 زمن، تفصيلة لكل محادثة |
| `DISABLE_AUTOUPDATER=1` | `.env.example` | ترقية الـ CLI بتكسر الكاش وبتعيد معالجة الجلسات المستأنفة بالكامل — الترقية بقت قرار مجدول |
| `claude-agent-sdk>=0.2.122` | `requirements.txt` | أقل نسخة متحقق فيها من دعم `exclude_dynamic_sections` |

**اللي متغيرش:** سلوك هادي الظاهر — الأقسام المشالة من الـ system prompt بتتحقن أوتوماتيك في أول user message، فهادي لسه شايف نفس المعلومات.

### التحقق بعد النشر (3 رسايل)

1. رسالة تجربة → في اللوج: `cache_write` كبير و`cache_read` صغير (بناء الكاش — طبيعي).
2. رسالة تانية في نفس القناة جوه ساعة → `cache_read` كبير و`hit%` عالي.
3. «هادي احفظ إن …» (بتعمل git commit) ثم رسالة رابعة → **`hit%` لازم يفضل عالي**.
   قبل التعديل ده كان بيصفّر — دي علامة نجاح الإصلاح نفسها.
   لو `cache_write` فضل عالي باستمرار: نسخة الـ CLI قديمة وبتتجاهل الخيار → `claude update` مرة واحدة ثم ثبّت.

### Rollback

شيل سطر `"exclude_dynamic_sections": True` من `hadi_engine.py` وارجع شغّل — اللوج والتقرير مالهمش أي أثر على السلوك.

---

## بند 3.3 — فك القفل العالمي: تسلسل الكتابات المشتركة

التوازي نفسه اتفعّل في الترحيل (قفل لكل محادثة + `HADI_MAX_CONCURRENCY`) — البند ده بيقفل
الفجوة المتبقية: **سباقات الكتابة على الحالة المشتركة** بين محادثتين شغالين مع بعض.

| التغيير | الملف | الأثر |
|---------|-------|-------|
| `state_lock.py` (جديد) | قفل `flock` واحد (`.locks/hadi-state.lock`) | طابور كتابة واحد بين *العمليات* — بيشتغل مع الـ subprocesses اللي asyncio.Lock مش شايفها، وبيتفك أوتوماتيك لو العملية وقعت |
| `memory.py` | أمر `add` جوه القفل | مفيش تصادم git index.lock ولا ضياع سطر ذاكرة بين محادثتين |
| `schedule.py` | `add`/`cancel` جوه القفل | كتابات reminders.json متسلسلة |
| `discord_bot.py` | تعليم المنفَّذ في `reminder_loop` جوه نفس القفل (في thread) | إضافة تذكير أثناء التعليم مش بتتمسح |
| `hadi_engine.py` | mutex على sessions.json | مفيش تسجيلة جلسة بتضيع بين رسالتين متزامنتين |

**التحقق:** ابعت في نفس اللحظة «هادي احفظ إن كذا» في قناتين مختلفتين → الاتنين يتحفظوا
وcommit ورا commit في git log (من غير WARN عن index.lock)، و`ls .locks/` هتلاقي
`hadi-state.lock` (ملف تشخيص فيه آخر PID مسك القفل).

---

## بند 3.4 — Streaming UX: رسالة الحالة الحية

بدل رسالة «⏳» الثابتة اللي كانت بتظهر بعد 20 ثانية: رسالة حالة **فورية** بتتحدث
بالنشاط الحقيقي للمحرك والزمن المنقضي، وبتتمسح مع وصول الرد.

- **المصدر:** بلوكات الرسايل في `hadi_engine` (ToolUseBlock/TextBlock) → وصف مصري
  مختصر («بكلم Azure DevOps» / «ببحث على النت» / «بكتب الرد») عبر `on_progress`.
- **rate limits:** تعديل واحد كحد أقصى كل `HADI_STATUS_EDIT_INTERVAL` ثانية (افتراضي 5)
  + ticker كل 15 ثانية للزمن — تحت حد Discord (~5 تعديلات/5 ثواني) بهامش كبير.
- **NO_REPLY:** الرسالة بتتمسح في كل الحالات — اخترنا «رسالة حالة» مش «تحرير الرد
  بالمحتوى» عشان قرار الصمت يفضل صامت فعلًا ومفيش محتوى جزئي بيتسرب.
- **في الطابور:** لو في طلب شغال قبلك في نفس المحادثة الرسالة بتقول كده صراحة،
  ولو الطابور العام (semaphore) كله مشغول بتقول «مستني تشغيلة تانية تخلص».

**التحقق:** اطلب تقرير طويل → رسالة «⏳ ماشي…» فورية، بتتبدل بالنشاطات، وبتختفي مع
الرد. ابعت رسالة هزار بين الزمايل (NO_REPLY) → رسالة الحالة تظهر وتختفي من غير أي رد.

---

## بند 4.1/4.2 — الذاكرة المتدرجة (مرحلة أ+ب)

**المشكلة (4.1):** memory.md كان بيتحقن كامل في كل رسالة — data bottleneck معلن،
هيكبر مع كل «احفظ». **الحل (4.2):** نواة ثابتة + استرجاع بالصلة + أنواع وصلاحية —
من غير أي خدمة خارجية (SQLite FTS5 + BM25 بتطبيع عربي).

| المكوّن | الدور |
|---------|-------|
| `knowledge/memory_core.md` (جديد — في git) | نواة يدوية بتتحقن في كل رسالة دايمًا |
| `knowledge/memory_index.db` (مشتق — في .gitignore) | فهرس FTS5 بيتبني من memory.md بـ `rebuild` في أي وقت |
| `memory_store.py` (جديد) | المحرك: rebuild / search / block / status / selftest |
| `memory.py` | حفظ مزدوج (ملف + فهرس) + `--type` و`--expires` + بحث BM25 بدل substring |
| `discord_bot.py` | `get_memory_context()`: نواة + قواعد سلوك + ملاحظات — وfallback للحقن الكامل لو الفهرس وقع |

**أنواع الذاكرة (مرحلة ب):** `semantic` دائمة (الافتراضي) • `episodic` بصلاحية
(`--expires YYYY-MM-DD` — بتسقط من الحقن بعد تاريخها وبتفضل في الملف أرشيف) •
`procedural` قواعد سلوك (بتتحقن دايمًا زي النواة).

**سلوك اليوم الأول:** الذاكرة لسه صغيرة (تحت `HADI_MEMORY_FULL_LIMIT` = 4000 حرف)
→ بتتحقن كاملة زي ما هي = **صفر تغيير في السلوك الظاهر**. التدرج لأقرب
`HADI_MEMORY_TOP_K` = 5 ملاحظات بيشتغل أوتوماتيك أول ما تكبر. المنتهي بيختفي فورًا.

**التحقق:** `python3 memory_store.py selftest` ← PASS، ثم `rebuild` و`status`
(in_sync: True)، و`block --query "..."` لمعاينة اللي بيتحقن بالظبط.

**Rollback:** رجّع سطر `mem = load_memory()` في discord_bot.py — أو سيبه: أي فشل
في الفهرس بيرجع للحقن الكامل لوحده مع WARN في اللوج.
